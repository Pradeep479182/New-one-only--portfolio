import { motion } from 'framer-motion'
import { Braces, Database, LayoutTemplate, Wrench } from 'lucide-react'
import SectionTitle from './SectionTitle'
const groups = [{ icon: LayoutTemplate, label: 'Frontend', items: ['React.js', 'Next.js', 'JavaScript', 'TypeScript', 'HTML / CSS'] }, { icon: Braces, label: 'Backend', items: ['Node.js', 'Next.js API', 'REST APIs', 'Python'] }, { icon: Database, label: 'Data & AI', items: ['SQL Server', 'MySQL', 'MongoDB', 'AI workflows'] }, { icon: Wrench, label: 'Toolbox', items: ['Git / GitHub', 'Figma', 'VS Code', 'Vercel'] }]
export default function Skills() { return <section id="skills" className="section skills-section"><div className="page-width"><SectionTitle eyebrow="02 / toolkit" title="The tools behind the work." intro="A flexible stack for bringing ideas from the first sketch to production." /><div className="skills-grid">{groups.map(({ icon: Icon, label, items }, i) => <motion.article className="skill-card" key={label} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ delay: i * .1 }} viewport={{ once: true }}><Icon size={21} /><span className="skill-index">0{i + 1}</span><h3>{label}</h3><ul>{items.map((item) => <li key={item}>{item}</li>)}</ul></motion.article>)}</div></div></section> }
