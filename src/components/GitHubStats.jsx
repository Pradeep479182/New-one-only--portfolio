import { Activity, ArrowUpRight, Code2, GitCommitHorizontal, Star } from 'lucide-react'
import SectionTitle from './SectionTitle'
export default function GitHubStats({ github, username }) { const user = github.user; const events = github.events.length ? github.events.slice(0, 3) : [{ id: 1, type: 'PushEvent', repo: { name: 'portfolio-lab' } }, { id: 2, type: 'CreateEvent', repo: { name: 'api-experiments' } }]; return <section id="github" className="section page-width github-section"><SectionTitle eyebrow="05 / live signal" title="The code is always moving." intro="A daily snapshot pulled directly from GitHub. This portfolio refreshes automatically every 24 hours." /><div className="github-panel"><div className="github-profile"><div className="github-icon"><Code2 size={28} /></div><div><span className="eyebrow">github / {username}</span><h3>{user?.name || 'Pradeep Rakavi'}</h3><p>{user?.bio || 'Building, learning, and sharing in public.'}</p></div><a className="icon-link" href={`https://github.com/${username}`} target="_blank" rel="noreferrer" aria-label="Open GitHub profile"><ArrowUpRight size={18} /></a></div><div className="github-metrics"><div><span>public repos</span><strong>{user?.public_repos || '12'}</strong></div><div><span>followers</span><strong>{user?.followers || '24'}</strong></div><div><span>contributions</span><strong className="metric-live"><Activity size={15} /> active</strong></div></div><div className="activity-list"><div className="activity-heading"><span>recent activity</span><span className="sync-state"><i /> synced today</span></div>{events.map((event) => <div className="activity-row" key={event.id}><GitCommitHorizontal size={16} /><span>{event.type === 'PushEvent' ? 'Pushed updates to' : 'Activity on'} <strong>{event.repo?.name?.split('/').pop()}</strong></span><span className="activity-star"><Star size={13} /> live</span></div>)}</div></div></section> }
